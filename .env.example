# Optional overrides for Makefile variables
# Copy to .env and adjust as needed.

# Optional Docker image name prefix (defaults to "mcp-" when empty)
image_prefix=

# Optional server-name prefix for VS Code MCP config keys
# Leave empty to avoid a default prefix
mcp_name_prefix=
